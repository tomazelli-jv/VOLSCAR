# Database Configuration - Update these values with your Hostinger credentials
DB_HOST=localhost
DB_USER=seu_usuario_mysql
DB_PASSWORD=uma_senha_mysql_forte
DB_NAME=seu_banco_mysql
DB_PORT=3306

# JWT Secret - Change this to a strong random value
JWT_SECRET=gere_um_segredo_aleatorio_com_pelo_menos_64_bytes

# Origin allowed to call the private API
APP_ORIGIN=https://seu-dominio.com

# Optional JWT hardening
JWT_ISSUER=volscar
JWT_AUDIENCE=volscar-web

# Server Port
PORT=3000
NODE_ENV=production


